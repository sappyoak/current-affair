export enum CommandEvents {
    ExecutionStart,
    ExecutionSuccess,
    ExecutionFailure,
    ExecutionTimeout,
    SemaphoreRejected,
    SemaphoreQueued,
    ShortCircuited,
    FallbackStart,
    FallbackSuccess,
    FallbackFailure,
    FallbackTimeout,
    FallbackMissing,
    CommandStart,
    CommandSuccess,
    CommandFailure
}
